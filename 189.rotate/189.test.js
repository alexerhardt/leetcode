const rotate = require('./189.main.js');

const data = [

];

let testMap = new Map(data);

for (let [key, value] of testMap.entries()) {
    test(`${key} = ${value}`, () => {
        expect(rotate(key)).toBe(value);
    });
}
